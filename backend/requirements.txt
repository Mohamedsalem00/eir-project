# backend/requirements.txt
# Core FastAPI and web framework
fastapi>=0.104.0,<0.115.0
uvicorn[standard]>=0.24.0

# Database and ORM
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0
alembic>=1.12.0

# Authentication and Security
python-jose[cryptography]>=3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1

# Data validation and parsing
pydantic>=2.0.0,<2.5.0
pydantic[email]

# Environment and configuration
python-dotenv>=1.0.0

# Internationalization
babel>=2.13.0

# File handling and forms
python-multipart>=0.0.6

# Additional utilities for enhanced features
requests>=2.31.0
python-dateutil>=2.8.0
pandas>=2.0.0
chardet>=5.2.0,<6.0.0

# YAML configuration support for multi-protocol integration
PyYAML>=6.0.0

# APIs externes
aiohttp>=3.8.0

# Notifications system dependencies
# APScheduler for background task scheduling
APScheduler>=3.10.0

# Email support
# Built-in smtplib is used, no additional packages needed for basic SMTP

# SMS support (optional providers)
# twilio>=8.5.0  # Uncomment for Twilio SMS support
# boto3>=1.28.0  # Uncomment for AWS SNS SMS support

# Additional utilities for notifications
pytz>=2023.3  # Timezone support for scheduler


starlette-admin

# Admin dashboard for FastAPI
sqladmin>=0.13.0
# Session support for SQLAdmin (required)
itsdangerous>=2.1.2
